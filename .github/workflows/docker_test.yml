name: Docker test

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
      
    - uses: actions/checkout@v4
    - name: running docker compose up --build
      run: docker-compose -f "docker-compose.yml" up -d --build

    - uses: actions/checkout@v4
    - name: running the unit test frontend
      run: docker-compose -f "docker-compose.yml" run frontend npm run test

    - name: Stop containers
      if: always()
      run: docker-compose -f "docker-compose.yml" down
   
